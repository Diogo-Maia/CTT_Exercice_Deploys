apiVersion: apps/v1
kind: Deployment
metadata:
  name: exercice-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exercice-api
  template:
    metadata:
      labels:
        app: exercice-api
    spec:
      containers:
        - name: exercice-api
          image: issamaia/exercice-api:latest
          ports:
            - containerPort: 80
          env:
            - name: ConnectionStrings__MongoDb
              value: "mongodb://mongo:27017"
---
apiVersion: v1
kind: Service
metadata:
  name: exercice-api
spec:
  selector:
    app: exercice-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
# Updated at Sun Jun  1 14:36:17 UTC 2025
